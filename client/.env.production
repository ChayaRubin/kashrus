# Used when running: npm run build for the Android app
# Point the Android app to your local backend on the host machine.
# 10.0.2.2 = the host (your PC) as seen from the Android emulator.
VITE_API_URL=http://10.0.2.2:5000
